import { SearchrticlePipe } from './searchrticle.pipe';

describe('SearchrticlePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchrticlePipe();
    expect(pipe).toBeTruthy();
  });
});
